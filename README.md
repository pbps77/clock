# 시계 (HTML + JS)

간단한 웹 시계입니다. 연(년), 월, 일, 시:분:초, 요일을 실시간으로 표시합니다.

실행 방법:
1. 이 폴더(`index.html`)를 웹 브라우저로 열면 됩니다.
   - 파일을 더블클릭하거나 브라우저에서 `파일 열기`로 선택하세요.

구성 파일:
- `index.html` : 마크업
- `style.css` : 스타일
- `script.js` : 시계 로직 (1초마다 갱신)

추가 아이디어:
- 12시간/24시간 토글 추가
- 로컬 저장소에 포맷 설정 저장
- 시간대 선택 기능
